<UserControl x:Class="ConfigEditor.App.Views.ConfigTreeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:ConfigEditor.App.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="300">
    <Grid Background="{DynamicResource PanelBgBrush}">
        <TreeView ItemsSource="{Binding SelectedDocument.Nodes}"
                  SelectedItemChanged="TreeView_SelectedItemChanged"
                  BorderThickness="0"
                  Background="Transparent"
                  Margin="10">
            <TreeView.ItemContainerStyle>
                <Style TargetType="TreeViewItem" BasedOn="{StaticResource {x:Type TreeViewItem}}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                    <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}"/>
                </Style>
            </TreeView.ItemContainerStyle>
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate DataType="{x:Type vm:ConfigNodeViewModel}" ItemsSource="{Binding Children}">
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <Border x:Name="TypeBadge" CornerRadius="3" Padding="4,1" Margin="0,0,6,0" VerticalAlignment="Center">
                            <TextBlock x:Name="TypeText" Text="" FontSize="9" FontWeight="Bold" Foreground="White"/>
                        </Border>
                        <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <HierarchicalDataTemplate.Triggers>
                        <DataTrigger Binding="{Binding ValueType}" Value="String">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#262682"/>
                            <Setter TargetName="TypeText" Property="Text" Value="str"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValueType}" Value="Integer">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#DA9274"/>
                            <Setter TargetName="TypeText" Property="Text" Value="int"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValueType}" Value="Float">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#B8738D"/>
                            <Setter TargetName="TypeText" Property="Text" Value="flt"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValueType}" Value="Boolean">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#6969B8"/>
                            <Setter TargetName="TypeText" Property="Text" Value="bool"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValueType}" Value="Object">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#1B1B66"/>
                            <Setter TargetName="TypeText" Property="Text" Value="obj"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValueType}" Value="Array">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#C47F66"/>
                            <Setter TargetName="TypeText" Property="Text" Value="arr"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValueType}" Value="Null">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#77728A"/>
                            <Setter TargetName="TypeText" Property="Text" Value="null"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding ValueType}" Value="Unknown">
                            <Setter TargetName="TypeBadge" Property="Background" Value="#77728A"/>
                            <Setter TargetName="TypeText" Property="Text" Value="unk"/>
                        </DataTrigger>
                    </HierarchicalDataTemplate.Triggers>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>
    </Grid>
</UserControl>
